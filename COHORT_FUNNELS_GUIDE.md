# Руководство: Генерация воронок на основе когорт

## 📋 Описание

Новая функция автоматически создает воронки конверсии для каждой когорты пользователей на основе их реального поведения на сайте. Это позволяет:

- **Персонализировать анализ** - каждая когорта получает свои специфичные воронки
- **Автоматизировать создание** - не нужно вручную создавать воронки для каждой группы
- **Выявлять проблемы** - понимать, где именно теряются пользователи каждой когорты

## 🔧 Как это работает

1. **Анализ путей пользователей когорты**: Система извлекает все последовательности URL и достигнутых целей для пользователей конкретной когорты
2. **Поиск частых последовательностей**: Находит наиболее часто встречающиеся пути (минимум 3 пользователя или 10% от когорты)
3. **Создание воронок**: Преобразует найденные последовательности в воронки с шагами типа `url` и `goal`
4. **Валидация**: Проверяет качество воронок (минимум 2 шага, наличие URL, уникальность)

## 🚀 Использование

### Базовое использование

```powershell
# Создать воронки для всех когорт версии
docker-compose exec web python manage.py generate_cohort_funnels --product-version "v2.0 (2024)"
```

### Параметры команды

```powershell
docker-compose exec web python manage.py generate_cohort_funnels `
    --product-version "v2.0 (2024)" `
    --min-support 3 `
    --max-funnels-per-cohort 3 `
    --min-length 2 `
    --max-length 5 `
    --cohort-id 123 `
    --clear-auto `
    --dry-run
```

**Параметры:**
- `--min-support` - Минимум пользователей для воронки (по умолчанию: 3)
- `--max-funnels-per-cohort` - Максимум воронок на когорту (по умолчанию: 3)
- `--min-length` - Минимальная длина пути (по умолчанию: 2)
- `--max-length` - Максимальная длина пути (по умолчанию: 5)
- `--cohort-id` - Обработать только конкретную когорту (опционально)
- `--clear-auto` - Удалить старые автоматические воронки перед созданием
- `--dry-run` - Показать найденные воронки без создания в БД

### Примеры

#### 1. Создать воронки для всех когорт

```powershell
docker-compose exec web python manage.py generate_cohort_funnels `
    --product-version "v2.0 (2024)" `
    --max-funnels-per-cohort 5
```

#### 2. Тестовый запуск (dry-run)

```powershell
docker-compose exec web python manage.py generate_cohort_funnels `
    --product-version "v2.0 (2024)" `
    --dry-run
```

#### 3. Обработать только одну когорту

```powershell
docker-compose exec web python manage.py generate_cohort_funnels `
    --product-version "v2.0 (2024)" `
    --cohort-id 5
```

#### 4. Очистить старые и создать новые

```powershell
docker-compose exec web python manage.py generate_cohort_funnels `
    --product-version "v2.0 (2024)" `
    --clear-auto
```

## 📊 После создания воронок

После генерации воронок рекомендуется рассчитать их метрики:

```powershell
# Рассчитать метрики для всех воронок с разбивкой по когортам
docker-compose exec web python manage.py calculate_funnels `
    --product-version "v2.0 (2024)" `
    --by-cohorts
```

## 🎯 Что создается

Для каждой когорты создаются воронки с названиями вида:
- `"Название когорты: Шаг 1 → Шаг 2"`
- `"Название когорты: Шаг 1 → ... → Шаг N"`

Примеры:
- `"Заблудившиеся с мобилок: Главная → Программы → Форма заявки"`
- `"Заинтересованные в поступлении: Главная → Контакты → Кнопка IT-магистратура"`

## ⚙️ Технические детали

### Алгоритм работы

1. **Извлечение путей** (`extract_user_paths_with_goals`):
   - Получает все сессии пользователей когорты
   - Строит последовательности из URL (hits) и целей (goals_id из сессий)
   - Нормализует URL и убирает дубликаты

2. **Поиск частых последовательностей** (`find_frequent_sequences_with_goals`):
   - Генерирует все подпоследовательности длины 2-5
   - Считает частоту каждой последовательности
   - Фильтрует по минимальной поддержке

3. **Фильтрация**:
   - Убирает подпоследовательности, если есть более длинная с такой же частотой
   - Валидирует: минимум 2 шага, наличие URL, уникальность

4. **Создание воронок** (`sequences_to_funnels_with_goals`):
   - Преобразует последовательности в формат воронок
   - Создает шаги типа `url` и `goal`
   - Генерирует названия и описания

### Адаптивные параметры

- **min_support**: Автоматически адаптируется к размеру когорты
  - Минимум: 3 пользователя
  - Или: 10% от размера когорты (что больше)

### Ограничения

- Воронки создаются только для когорт с `member_client_ids`
- Минимальная длина пути: 2 шага
- Максимальная длина пути: 5 шагов (по умолчанию)
- Воронка должна содержать хотя бы один URL шаг

## 🔍 Отладка

### Проверка когорт

```powershell
# Зайти в Django shell
docker-compose exec web python manage.py shell
```

```python
from analytics.models import UserCohort, ProductVersion

version = ProductVersion.objects.get(name="v2.0 (2024)")
cohorts = UserCohort.objects.filter(version=version)

for cohort in cohorts:
    print(f"{cohort.name}: {cohort.users_count} пользователей, {len(cohort.member_client_ids)} client_ids")
```

### Проверка созданных воронок

```powershell
# Зайти в Django shell
docker-compose exec web python manage.py shell
```

```python
from analytics.models import ConversionFunnel, ProductVersion

version = ProductVersion.objects.get(name="v2.0 (2024)")
funnels = ConversionFunnel.objects.filter(
    version=version,
    is_preset=False,
    name__contains=':'
)

for funnel in funnels:
    print(f"{funnel.name}: {len(funnel.steps)} шагов")
```

## 📝 Примеры вывода

```
🔍 Генерация воронок для когорт версии "v2.0 (2024)"...
   Параметры: min_support=3, длина пути 2-5, максимум 3 воронок на когорту
   Найдено когорт: 5

📊 Когорта 1/5: "Заблудившиеся с мобилок"
   Пользователей: 234 (12.3%)
   Извлечено путей: 156
   Найдено последовательностей: 8
   ✅ Найдено воронок: 3

      1. Заблудившиеся с мобилок: Главная → Программы → Форма заявки
         Путь: Главная → Программы → Форма заявки
         Частота: 45 пользователей

      2. Заблудившиеся с мобилок: Главная → Контакты
         Путь: Главная → Контакты
         Частота: 38 пользователей

      3. Заблудившиеся с мобилок: Программы → Кнопка IT-магистратура → Форма заявки
         Путь: Программы → Кнопка IT-магистратура → Форма заявки
         Частота: 23 пользователя

   ✅ Создано воронок: 3

============================================================
📊 Итоговая статистика:
   Обработано когорт: 5
   Когорт с воронками: 4
   Создано воронок: 12
   Пропущено воронок: 0
============================================================
```

## 🐛 Решение проблем

### Воронки не создаются

1. **Проверьте наличие когорт**:
   ```powershell
   docker-compose exec web python manage.py shell
   ```
   ```python
   from analytics.models import UserCohort, ProductVersion
   version = ProductVersion.objects.get(name="v2.0 (2024)")
   print(UserCohort.objects.filter(version=version).count())
   ```

2. **Проверьте наличие данных**:
   - Убедитесь, что данные загружены через `ingest_data`
   - Проверьте, что когорты имеют `member_client_ids`

3. **Уменьшите min-support**:
   ```powershell
   docker-compose exec web python manage.py generate_cohort_funnels `
       --product-version "v2.0 (2024)" `
       --min-support 2
   ```

### Слишком много/мало воронок

- **Увеличьте max-funnels-per-cohort** для большего количества
- **Увеличьте min-support** для меньшего количества (более строгий фильтр)

### Воронки содержат только цели без URL

Это нормально, если пользователи когорты в основном достигают целей без навигации. Но такие воронки фильтруются валидацией (нужен хотя бы один URL).

## 🔗 Связанные команды

```powershell
# Загрузка данных и создание когорт
docker-compose exec web python manage.py ingest_data `
    --visits "2024_yandex_metrika_visits.parquet" `
    --hits "2024_yandex_metrika_hits.parquet" `
    --product-version "v2.0 (2024)" `
    --year 2024

# Обнаружение воронок для всех пользователей (не по когортам)
docker-compose exec web python manage.py discover_funnels `
    --product-version "v2.0 (2024)"

# Расчет метрик воронок
docker-compose exec web python manage.py calculate_funnels `
    --product-version "v2.0 (2024)" `
    --by-cohorts

# Создание воронок вручную
docker-compose exec web python manage.py create_funnels `
    --product-version "v2.0 (2024)"
```

