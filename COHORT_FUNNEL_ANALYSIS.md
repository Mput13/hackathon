# Анализ идеи: Создание воронок на основе когорт

## 📋 Суть идеи

**Предложение**: На основе когорт (которые определяются по поведению пользователей и достижению таргетов) автоматически определить и создать воронки, специфичные для каждой когорты.

## 🔍 Текущее состояние системы

### Что уже есть:

1. **Когорты (UserCohort)**:
   - Определяются через K-Means кластеризацию на основе:
     - Метрик поведения (duration, depth, visits, bounce)
     - Достигнутых целей (goals)
     - Интересов (rating, news, contacts, admission, forms, programs)
   - Хранят `member_client_ids` - список пользователей в когорте
   - Хранят `conversion_rates` - какие цели достигались чаще всего
   - Имеют AI-генерированные названия (например, "Заблудившиеся с мобилок")

2. **Воронки (ConversionFunnel)**:
   - Могут состоять из шагов типа `goal` или `url`
   - Есть автоматическое обнаружение воронок (`funnel_discovery.py`), но работает на **всех** пользователях
   - Есть расчет метрик по когортам (`calculate_funnel_metrics_by_cohorts`)

3. **Данные**:
   - `VisitSession` - сессии пользователей с `goals_id`
   - `PageHit` - все действия пользователей (URL, timestamp, page_title)
   - Полная история навигации по сайту

## ✅ Реализуемость: **ВЫСОКАЯ** (9/10)

### Технические возможности:

1. **Доступ к данным когорт**: ✅
   - `cohort.member_client_ids` - список всех пользователей когорты
   - Можно фильтровать сессии и hits по этим client_ids

2. **Анализ путей пользователей**: ✅
   - Уже реализовано в `funnel_discovery.py`:
     - `extract_user_paths()` - извлекает последовательности URL
     - `find_frequent_sequences()` - находит частые пути
   - Можно адаптировать для работы с фильтром по когортам

3. **Создание воронок**: ✅
   - Модель `ConversionFunnel` поддерживает создание через код
   - Есть примеры в `create_funnels.py` и `discover_funnels.py`

4. **Интеграция с целями**: ✅
   - Когорты хранят `conversion_rates` - какие цели достигались
   - Можно использовать эти цели как шаги воронок
   - `GoalParser` уже умеет работать с целями

### Что нужно реализовать:

1. **Функция обнаружения воронок для когорты**:
   ```python
   def discover_funnels_for_cohort(cohort: UserCohort, version: ProductVersion) -> List[Dict]
   ```
   - Фильтровать сессии по `cohort.member_client_ids`
   - Извлекать пути только для этой когорты
   - Находить частые последовательности
   - Создавать воронки с названием, связанным с когортой

2. **Комбинирование URL и целей**:
   - Текущий `funnel_discovery.py` работает только с URL
   - Нужно добавить поддержку целей из `conversion_rates`
   - Создавать шаги типа `goal` для важных целей когорты

3. **Команда/API для генерации**:
   - Management command: `docker-compose exec web python manage.py generate_cohort_funnels --product-version "v2.0 (2024)"`
   - Или автоматически при создании когорт

## 💡 Полезность: **ОЧЕНЬ ВЫСОКАЯ** (9.5/10)

### Преимущества:

1. **Персонализация анализа**:
   - Каждая когорта имеет свои паттерны поведения
   - Воронки покажут специфичные пути для каждой группы
   - Например: "Заблудившиеся с мобилок" → воронка покажет, где именно они теряются

2. **Автоматизация**:
   - Не нужно вручную создавать воронки для каждой когорты
   - Система сама обнаружит важные пути
   - Экономия времени аналитиков

3. **Бизнес-ценность**:
   - **Выявление проблемных мест** для конкретных сегментов
   - **Оптимизация конверсии** - понимание, где теряются пользователи каждой когорты
   - **Персонализация UX** - можно оптимизировать сайт под разные когорты

4. **Синергия с существующими функциями**:
   - Уже есть расчет метрик воронок по когортам
   - Теперь можно автоматически создавать воронки для когорт
   - Полный цикл: когорты → воронки → метрики → инсайты

### Примеры использования:

1. **Когорта "Заинтересованные в поступлении"**:
   - Автоматически создастся воронка: Главная → Программы → Форма заявки → Отправка
   - Покажет, на каком шаге теряются потенциальные абитуриенты

2. **Когорта "Мобильные пользователи"**:
   - Воронка покажет специфичные проблемы мобильной навигации
   - Можно сравнить с десктопной когортой

3. **Когорта "Высокая конверсия"**:
   - Воронка покажет оптимальный путь успешных пользователей
   - Можно использовать как эталон для других когорт

## ⚙️ Сложность: **СРЕДНЯЯ** (6/10)

### Легкие части:

1. **Адаптация существующего кода**: 
   - `funnel_discovery.py` уже реализует большую часть логики
   - Нужно только добавить фильтр по `client_ids`

2. **Создание воронок**:
   - Простая операция через ORM
   - Есть примеры в коде

### Средние части:

1. **Комбинирование URL и целей**:
   - Нужно анализировать не только URL, но и достижение целей
   - Определить порядок: URL → Goal → URL → Goal
   - Требует дополнительной логики

2. **Нормализация и фильтрация**:
   - Определить, какие последовательности значимы
   - Убрать шум (случайные переходы)
   - Выбрать оптимальные параметры (min_support, min_length)

3. **Названия воронок**:
   - Связать название воронки с когортой
   - Использовать AI для генерации описаний (опционально)

### Сложные части:

1. **Производительность**:
   - Для каждой когорты нужно обработать все сессии
   - При большом количестве когорт может быть медленно
   - Решение: кэширование, оптимизация запросов, фоновые задачи

2. **Качество обнаруженных воронок**:
   - Могут быть ложные паттерны
   - Нужна валидация и фильтрация
   - Возможно, нужен порог значимости

3. **Управление дубликатами**:
   - Одна и та же воронка может быть обнаружена для разных когорт
   - Нужна логика дедупликации или объединения

## 📊 Оценка по критериям

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **Реализуемость** | 9/10 | Почти все компоненты уже есть, нужна адаптация |
| **Полезность** | 9.5/10 | Очень высокая бизнес-ценность, автоматизация |
| **Сложность** | 6/10 | Средняя, но есть готовые решения |
| **Время реализации** | 2-3 дня | При полной занятости |
| **Риски** | Низкие | Можно начать с простой версии и улучшать |

## 🎯 Рекомендации по реализации

### Фаза 1: MVP (Минимально жизнеспособный продукт)
1. Адаптировать `funnel_discovery.py` для работы с когортами
2. Создавать воронки только на основе URL (без целей)
3. Простое название: "{Cohort Name} - Auto Funnel"
4. Одна воронка на когорту (самая частая последовательность)

### Фаза 2: Улучшения
1. Добавить поддержку целей в воронки
2. Создавать несколько воронок на когорту (топ-3 последовательности)
3. AI-генерация названий и описаний
4. Фильтрация и валидация воронок

### Фаза 3: Оптимизация
1. Кэширование результатов
2. Фоновые задачи для генерации
3. Сравнение воронок между когортами
4. Автоматическое обновление при изменении когорт

## 🔧 Технический подход

### Алгоритм:

1. **Для каждой когорты**:
   ```python
   cohort_client_ids = set(cohort.member_client_ids)
   sessions = VisitSession.objects.filter(
       version=version,
       client_id__in=cohort_client_ids
   )
   ```

2. **Извлечь пути**:
   - Получить все hits для сессий когорты
   - Построить последовательности URL + Goals
   - Нормализовать пути

3. **Найти частые последовательности**:
   - Использовать существующий алгоритм из `funnel_discovery.py`
   - Адаптировать параметры для меньшего объема данных

4. **Создать воронки**:
   - Преобразовать последовательности в шаги воронки
   - Назвать: "{cohort.name} - {sequence_description}"
   - Сохранить в БД

5. **Валидация**:
   - Проверить, что воронка имеет минимум 2 шага
   - Проверить, что последовательность встречается достаточно часто
   - Избежать дубликатов

## 📝 Выводы

### ✅ Идея ОЧЕНЬ ХОРОШАЯ и РЕАЛИЗУЕМАЯ

**Почему стоит реализовать:**
- Высокая бизнес-ценность (персонализация, оптимизация)
- Технически реализуемо (есть все компоненты)
- Средняя сложность (можно начать с простой версии)
- Синергия с существующими функциями

**Рекомендация**: **РЕАЛИЗОВАТЬ** в 2-3 фазы, начиная с MVP.

**Приоритет**: **ВЫСОКИЙ** - это естественное развитие существующей функциональности.

