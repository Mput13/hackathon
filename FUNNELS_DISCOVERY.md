# Автоматическое обнаружение воронок

## Описание

Автоматическое обнаружение воронок анализирует реальные пути пользователей и находит наиболее частые последовательности URL. Это позволяет находить воронки, которые не были определены вручную, но реально используются пользователями.

## Как это работает

1. **Извлечение путей**: Анализируются все сессии пользователей, извлекаются последовательности URL
2. **Нормализация**: URL нормализуются (убираются параметры, ID, приводится к единому формату)
3. **Поиск частых последовательностей**: Используется n-gram анализ для поиска наиболее частых последовательностей длиной 2-4 шага
4. **Фильтрация**: Убираются избыточные подпоследовательности и слишком редкие пути
5. **Создание воронок**: Найденные последовательности преобразуются в конфигурации воронок

## Использование

### Базовое использование

```powershell
# Автоматически найти и создать воронки для версии 2024
docker-compose exec web python manage.py discover_funnels --product-version "v2.0 (2024)"
```

### С параметрами

```powershell
# Найти воронки с минимум 20 пользователями, максимум 15 воронок
docker-compose exec web python manage.py discover_funnels --product-version "v2.0 (2024)" --min-support 20 --max-funnels 15
```

### Параметры команды

- `--product-version` (обязательно): Название версии продукта
- `--min-support` (по умолчанию: 10): Минимальное количество пользователей для создания воронки
- `--max-funnels` (по умолчанию: 20): Максимальное количество воронок для создания
- `--min-length` (по умолчанию: 2): Минимальная длина пути
- `--max-length` (по умолчанию: 4): Максимальная длина пути
- `--clear-auto`: Удалить все автоматически созданные воронки перед обнаружением
- `--dry-run`: Показать найденные воронки без создания в БД

### Примеры

#### Dry-run (просмотр без создания)

```powershell
docker-compose exec web python manage.py discover_funnels --product-version "v2.0 (2024)" --dry-run
```

#### Удалить старые авто-воронки и создать новые

```powershell
docker-compose exec web python manage.py discover_funnels --product-version "v2.0 (2024)" --clear-auto --min-support 15
```

#### Найти только длинные воронки (3-5 шагов)

```powershell
docker-compose exec web python manage.py discover_funnels --product-version "v2.0 (2024)" --min-length 3 --max-length 5
```

## После обнаружения

После обнаружения воронок необходимо рассчитать их метрики:

```powershell
# Рассчитать метрики для всех воронок (включая автоматически созданные)
docker-compose exec web python manage.py calculate_funnels --product-version "v2.0 (2024)" --by-cohorts
```

## Отличия от preset-воронок

- **Preset-воронки** (`create_funnels`): Создаются вручную на основе анализа популярных страниц
- **Автоматически обнаруженные** (`discover_funnels`): Находятся автоматически на основе реальных путей пользователей

Оба типа воронок работают одинаково и могут использоваться одновременно.

## Оптимизация

Команда оптимизирована для работы с большими объемами данных:
- Использует `iterator()` для потоковой обработки сессий
- `prefetch_related()` для эффективной загрузки hits
- Группировка похожих URL (например, `/base/` и `/bachelor/` считаются эквивалентными)

## Пример вывода

```
🔍 Анализирую пути пользователей для версии "v2.0 (2024)"...
   Параметры: min_support=10, длина пути 2-4, максимум 20 воронок
   Извлекаю пути пользователей...
   Анализ завершен.

✅ Найдено воронок: 12

1. / → /rating/ → /rating/base/ (auto, 45 раз)
   Путь: Главная страница → Rating → Rating/base/
   Частота: 45 пользователей

2. / → /list/ → /list/base/ (auto, 32 раза)
   Путь: Главная страница → List → List/base/
   Частота: 32 пользователей

...
```


