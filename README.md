# UX Insight: AI-Powered UX Analytics Platform

Платформа для автоматизированного анализа пользовательского опыта на основе данных Яндекс.Метрики. Автоматически выявляет UX-проблемы, сравнивает версии продукта, анализирует воронки конверсии и генерирует гипотезы с помощью GigaChat AI.

Разработано в рамках хакатона PSB "AI Challenge: Banking".

## 📋 Содержание

- [Функциональность](#-функциональность)
- [Технологический стек](#-технологический-стек)
- [Установка и запуск](#-установка-и-запуск)
- [Архитектура системы](#-архитектура-системы)
- [Основные функции и их реализация](#-основные-функции-и-их-реализация)
- [API документация](#-api-документация)
- [Команды управления](#-команды-управления)
- [Структура проекта](#-структура-проекта)
- [Конфигурация](#-конфигурация)

## 🚀 Функциональность

### Основные возможности

* **ETL процесс**: Импорт данных из Parquet-файлов Яндекс.Метрики, нормализация, расчет метрик
* **Детекция UX-проблем**: Автоматическое выявление 10+ типов проблем пользовательского опыта
* **AI-инсайты**: Генерация гипотез и решений с помощью YandexGPT (GigaChat)
* **Воронки конверсии**: Создание, анализ и сравнение воронок с разбивкой по когортам
* **Сегментация пользователей**: K-Means кластеризация с AI-генерацией названий когорт
* **Сравнение версий**: Детальное сравнение метрик, проблем и когорт между версиями продукта
* **Веб-интерфейс**: Dashboard, Issues List, Compare, Funnels с интуитивным UI

### Типы выявляемых UX-проблем

1. **RAGE_CLICK** - Множественные клики по одному элементу за короткое время
2. **DEAD_CLICK** - Клики по неинтерактивным элементам или элементам с ошибками
3. **HIGH_BOUNCE** - Высокий процент отказов на страницах
4. **LOOPING** - Навигационные петли (A→B→A, A→B→C→A)
5. **NAVIGATION_BACK** - Частое использование кнопки "Назад"
6. **WANDERING** - Блуждание по сайту (10+ просмотров без достижения целей)
7. **FORM_ABANDON** - Брошенные формы
8. **FORM_FIELD_ERRORS** - Проблемы с заполнением форм (долгий ввод, множественные правки)
9. **FUNNEL_DROPOFF** - Критические точки отказа в воронках конверсии
10. **SCAN_AND_DROP** - Быстрый скролл до конца страницы с последующим выходом
11. **SEARCH_FAIL** - Неудачные поисковые запросы

## 🛠 Технологический стек

### Backend
* **Python 3.11** - Основной язык разработки
* **Django 5.0** - Web-фреймворк
* **PostgreSQL 15** - База данных
* **Pandas 2.0+** - Обработка данных
* **PyArrow** - Оптимизированное чтение Parquet
* **scikit-learn** - Машинное обучение (K-Means кластеризация)

### AI & External Services
* **YandexGPT (GigaChat)** - Генерация гипотез и решений через Yandex Cloud API

### Frontend
* **Tailwind CSS** - Стилизация
* **Chart.js** - Визуализация графиков
* **Django Templates** - Рендеринг страниц

### Infrastructure
* **Docker & Docker Compose** - Контейнеризация и оркестрация
* **PostgreSQL** - Реляционная БД

## 📦 Установка и запуск

### Предварительные требования

* Docker & Docker Compose (версия 2.0+)
* Файлы данных `.parquet`:
  * `2022_yandex_metrika_visits.parquet`
  * `2022_yandex_metrika_hits.parquet`
  * `2024_yandex_metrika_visits.parquet`
  * `2024_yandex_metrika_hits.parquet`

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/your-username/ux-insight.git
cd ux-insight
```

### Шаг 2: Подготовка файлов данных

Разместите Parquet-файлы в корне проекта (рядом с `manage.py`):

```
ux-insight/
├── manage.py
├── 2022_yandex_metrika_visits.parquet
├── 2022_yandex_metrika_hits.parquet
├── 2024_yandex_metrika_visits.parquet
└── 2024_yandex_metrika_hits.parquet
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Минимальная конфигурация `.env`:

```env
# Django
SECRET_KEY=your-secret-key-here
DEBUG=False

# Database (опционально, если используете внешнюю БД)
DB_HOST=db
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# Yandex Cloud AI (опционально, для AI-инсайтов)
FOLDER_ID=your-folder-id
API_KEY=your-api-key

# Автоматическая загрузка данных при старте
AUTO_INGEST=1
AUTO_FUNNELS=1
```

**Важно**: 
- Если `FOLDER_ID` и `API_KEY` не указаны, AI-функции будут использовать заглушки
- `AUTO_INGEST=1` автоматически загрузит данные при первом запуске
- `AUTO_FUNNELS=1` автоматически создаст и рассчитает preset-воронки

### Шаг 4: Запуск контейнеров

```bash
docker-compose up --build -d
```

Эта команда:
1. Соберет Docker-образы
2. Запустит PostgreSQL и Django приложение
3. Применит миграции БД
4. При `AUTO_INGEST=1` автоматически загрузит данные (если БД пустая)
5. При `AUTO_FUNNELS=1` создаст preset-воронки

### Шаг 5: Проверка статуса

Проверьте логи контейнеров:

```bash
docker-compose logs web
docker-compose logs db
```

### Шаг 6: Создание суперпользователя (опционально)

```bash
docker-compose exec web python manage.py createsuperuser
```

### Шаг 7: Доступ к приложению

Откройте браузер: [http://localhost:8000](http://localhost:8000)

## 🏗 Архитектура системы

### Общая схема

```
┌─────────────────┐
│  Parquet Files  │ (Yandex Metrica)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  ingest_data    │ → ETL процесс
│  (Management    │   - Загрузка данных
│   Command)      │   - Нормализация
└────────┬────────┘   - Расчет метрик
         │            - Детекция проблем
         ▼            - Сегментация когорт
┌─────────────────┐
│   PostgreSQL    │ ← База данных
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Django Views   │ → Web-интерфейс
│  + Templates    │   - Dashboard
└────────┬────────┘   - Issues List
         │            - Compare
         ▼            - Funnels
┌─────────────────┐
│  YandexGPT API  │ ← AI-инсайты
└─────────────────┘
```

### Модели данных

#### ProductVersion
Версия продукта для сравнения (например, "v1.0 (2022)", "v2.0 (2024)")

#### VisitSession
Сессия пользователя (Visit из Метрики):
- Базовые метрики: `duration_sec`, `bounced`, `page_views`
- Технические данные: `browser`, `os`, `screen_width/height`, `device_category`
- Цели: `goals_id` (JSON список ID достигнутых целей)

#### PageHit
Действие внутри сессии (Hit):
- URL, timestamp, page_title
- Метрики: `time_on_page`, `scroll_depth`, `is_exit`
- Технические данные: `browser`, `os`, `device_category`

#### UXIssue
Обнаруженная UX-проблема:
- Тип проблемы, severity, описание
- Метрики влияния: `affected_sessions`, `impact_score`
- AI-инсайты: `ai_hypothesis`, `ai_solution`
- Роутинг: `trend`, `priority`, `recommended_specialists`

#### UserCohort
Сегмент пользователей (результат K-Means кластеризации):
- Название (AI-генерированное)
- Метрики: `percentage`, `avg_bounce_rate`, `avg_duration`
- `member_client_ids` (JSON) - список client_id для фильтрации воронок

#### ConversionFunnel
Воронка конверсии:
- Название, описание, шаги (JSON)
- Настройки: `require_sequence`, `allow_skip_steps`
- Флаг `is_preset` (preset vs кастомная)

#### FunnelMetrics
Кэшированные метрики воронки:
- Общие метрики: `total_entered`, `total_completed`, `overall_conversion`
- Разбивка по шагам и когортам
- Флаг `includes_cohorts` для разделения общих и когортных метрик

#### PageMetrics
Агрегированные метрики по страницам:
- `total_views`, `unique_visitors`
- `exit_rate`, `avg_time_on_page`, `avg_scroll_depth`
- `dominant_device`, `dominant_cohort`

#### DailyStat
Дневная статистика по версиям:
- `total_sessions`, `total_bounces`, `bounce_rate`
- `avg_duration`

## 🔧 Основные функции и их реализация

### 1. ETL процесс (`ingest_data`)

**Файл**: `analytics/management/commands/ingest_data.py`

#### Этапы обработки:

1. **Загрузка данных из Parquet**
   ```python
   df_visits = pd.read_parquet(visits_path, columns=visits_columns)
   df_hits = pd.read_parquet(hits_path, columns=hits_columns)
   ```

2. **Нормализация client_id**
   - Приведение к единому формату
   - Обработка `counterUserIDHash` для связи hits с visits

3. **Создание VisitSession**
   - Batch-обработка (batch_size=10000) для оптимизации памяти
   - Парсинг `goalsID` (может быть список, строка списка или число)
   - Сохранение технических метрик (browser, OS, screen size)

4. **Создание PageHit**
   - Batch-обработка (batch_size=50000)
   - Связывание с VisitSession через client_id

5. **Расчет time_on_page**
   - Используется SQL window function для производительности
   - Расчет как разница между последовательными hits в сессии
   ```sql
   UPDATE analytics_pagehit ph1
   SET time_on_page = EXTRACT(EPOCH FROM (
       SELECT ph2.timestamp 
       FROM analytics_pagehit ph2 
       WHERE ph2.session_id = ph1.session_id 
         AND ph2.timestamp > ph1.timestamp 
       ORDER BY ph2.timestamp ASC LIMIT 1
   ) - ph1.timestamp)
   ```

6. **Расчет метрик страниц** (`calculate_page_metrics`)
   - Агрегация по URL: views, unique visitors, exit rate
   - Определение dominant device и page_title
   - Оптимизация: один запрос для всех страниц, затем группировка

7. **Детекция UX-проблем** (`run_analysis`)
   - Вызов отдельных детекторов для каждого типа проблемы
   - Расчет impact_score на основе метрик
   - Генерация AI-инсайтов

8. **Сегментация пользователей** (`segment_users_into_cohorts`)
   - K-Means кластеризация на основе поведения
   - AI-генерация названий когорт
   - Сохранение `member_client_ids` для фильтрации воронок

### 2. Детекция UX-проблем

**Файл**: `analytics/management/commands/ingest_analysis.py`

#### Алгоритмы детекции:

**RAGE_CLICK**:
- Группировка кликов по координатам (x, y) и URL
- Обнаружение 3+ кликов в радиусе 10px за 5 секунд
- Impact = количество кликов × 0.5

**DEAD_CLICK**:
- Клики без последующего перехода или действия
- Impact = количество кликов × 0.3

**HIGH_BOUNCE**:
- Страницы с bounce_rate > 70% и views > 20
- Impact = (bounce_rate - 70) × views / 100

**LOOPING**:
- Обнаружение паттернов A→B→A или A→B→C→A
- Канонизация (A↔B считается как один паттерн)
- Impact = количество петель × 1.0

**WANDERING**:
- Сессии с >10 просмотрами без достижения целей (goalsID пустой)
- Impact = количество сессий × 0.4

**FORM_ABANDON**:
- Формы с долгим временем заполнения (>60 сек) без завершения
- Impact = количество брошенных форм × 0.6

**FUNNEL_DROPOFF**:
- Анализ воронок на предмет аномальных оттоков на шагах
- Impact = процент оттока × количество пользователей / 10

### 3. AI-инсайты

**Файл**: `analytics/ai_service.py`

#### Реализация:

1. **Интеграция с YandexGPT**
   ```python
   url = "https://llm.api.cloud.yandex.net/foundationModels/v1/completion"
   headers = {
       "Authorization": f"Api-Key {API_KEY}",
       "x-folder-id": FOLDER_ID
   }
   ```

2. **Формирование промпта**
   - Системный промпт с контекстом проекта (приемная комиссия МАИ)
   - Пользовательский промпт с описанием проблемы и метриками
   - Расширенный контекст: page_title, dominant_cohort, dominant_device

3. **Формат ответа**
   - Строгий формат: "Гипотеза: ... Исправить: ..."
   - Парсинг ответа для разделения hypothesis и solution

4. **Fallback**
   - Если API недоступен, используются заглушки на основе типа проблемы

### 4. Воронки конверсии

**Файлы**: 
- `analytics/funnel_utils.py` - расчет метрик
- `analytics/management/commands/create_funnels.py` - создание preset-воронок
- `analytics/management/commands/calculate_funnels.py` - расчет метрик

#### Типы шагов воронок:

1. **URL-шаги**: Точное совпадение или prefix matching URL
2. **Goal-шаги**: Достижение цели из `goals.yaml`
   - `identifier`: Проверка через `goalsID` в сессии
   - `url_prefix`: URL начинается с заданного префикса
   - `url_contains`: URL содержит заданную строку

#### Алгоритм расчета метрик:

1. **Фильтрация сессий**
   - Поиск сессий, которые достигли первого шага воронки
   - Проверка последовательности шагов (если `require_sequence=True`)

2. **Подсчет метрик по шагам**
   ```python
   for step_idx, step in enumerate(funnel.steps):
       # Находим сессии, достигшие этого шага
       sessions_reached = filter_sessions_by_step(...)
       step_metrics.append({
           'step_name': step['name'],
           'reached': len(sessions_reached),
           'conversion_from_prev': conversion_rate
       })
   ```

3. **Разбивка по когортам**
   - Для каждой когорты фильтруются сессии по `member_client_ids`
   - Рассчитываются отдельные метрики для каждой когорты
   - Генерируется AI-анализ для каждой когорты

4. **Кэширование**
   - Метрики сохраняются в `FunnelMetrics` для быстрого доступа
   - Отдельные записи для общих метрик и метрик по когортам

#### Создание кастомных воронок:

1. **Web-интерфейс**: `/funnels/create/`
   - Форма с динамическим добавлением шагов
   - Выбор типа шага (URL или Goal)
   - Автодополнение целей из `goals.yaml`

2. **Валидация**
   - Минимум 2 шага
   - Проверка корректности URL/Goal

3. **Редактирование и удаление**
   - Только для кастомных воронок (preset защищены)

### 5. Сегментация пользователей (Когорты)

**Файл**: `analytics/management/commands/ingest_data.py` → `segment_users_into_cohorts`

#### Алгоритм:

1. **Сбор признаков пользователя**
   ```python
   user_behavior = df_visits.groupby('clientID').agg({
       'visitDuration': 'mean',  # Средняя длительность
       'pageViews': 'mean',      # Средняя глубина
       'visitID': 'count',      # Количество визитов
       'bounce': 'mean'         # Вероятность отказа
   })
   ```

2. **Достижение целей**
   - Для каждой цели из `goals.yaml` добавляется бинарный признак
   - Identifier goals: проверка через `goalsID`
   - URL goals: проверка через hits

3. **Интересы пользователя**
   - Анализ URL на наличие ключевых слов:
     - `rating`, `spis` → interest_rating
     - `news`, `novosti` → interest_news
     - `contact`, `kontak` → interest_contacts
     - `apply`, `priem` → interest_admission
     - и т.д.

4. **K-Means кластеризация**
   ```python
   scaler = StandardScaler()
   X = scaler.fit_transform(user_behavior[feature_cols])
   n_clusters = min(10, max(5, len(user_behavior)//30))
   kmeans = KMeans(n_clusters=n_clusters, random_state=42)
   user_behavior['cluster'] = kmeans.fit_predict(X)
   ```

5. **AI-генерация названий**
   - Для каждой когорты формируется описание метрик
   - Отправка в YandexGPT для генерации названия
   - Добавление деталей (интересы, цели) для уникальности

6. **Сохранение**
   - Сохранение `member_client_ids` для каждой когорты
   - Используется для фильтрации сессий в воронках

### 6. Сравнение версий

**Файл**: `analytics/views_compare.py` → `_build_comparison`

#### Что сравнивается:

1. **Общие метрики**
   - Visits, bounce rate, duration
   - Дельта между версиями

2. **UX Issues**
   - Индексация по (issue_type, normalized_url)
   - Статусы: new, worse, improved, stable, resolved
   - Расчет `impact_diff`

3. **Страницы**
   - Сравнение PageMetrics
   - Статусы: new, removed, changed, stable
   - Дельта exit_rate и avg_time_on_page
   - Фильтр малотрафиковых страниц (<20 views)

4. **Когорты**
   - Сравнение по названиям
   - Статусы: new, removed, changed
   - Изменение процента аудитории

5. **Устройства и браузеры**
   - Разбивка по device_category и browser
   - Доля трафика, bounce rate, duration
   - Дельта между версиями

## 🔌 API документация

Все API endpoints возвращают JSON.

### Версии

**GET** `/api/versions/`
```json
{
  "versions": [
    {
      "id": 1,
      "name": "v1.0 (2022)",
      "release_date": "2022-01-01",
      "is_active": true
    }
  ]
}
```

### Dashboard

**GET** `/api/dashboard/`
```json
{
  "version_stats": [
    {
      "id": 1,
      "name": "v1.0 (2022)",
      "total_visits": 3000,
      "avg_duration": 245.5,
      "bounce_rate": 45.2,
      "issue_count": 23,
      "critical_issues": 5
    }
  ],
  "recent_issues": [...]
}
```

### Сравнение версий

**GET** `/api/compare/?v1=1&v2=2`
```json
{
  "v1": {"id": 1, "name": "v1.0 (2022)"},
  "v2": {"id": 2, "name": "v2.0 (2024)"},
  "visits_diff": 500,
  "bounce_diff": -5.2,
  "duration_diff": 30.5,
  "stats_v1": {...},
  "stats_v2": {...},
  "issues_diff": [
    {
      "id": 1,
      "issue_type": "HIGH_BOUNCE",
      "status": "worse",
      "impact_diff": 2.5,
      "location_readable": "Главная страница"
    }
  ],
  "pages_diff": [...],
  "cohorts_diff": [...]
}
```

### Issues

**GET** `/api/issues/?version=1&severity=CRITICAL&issue_type=HIGH_BOUNCE`
```json
{
  "count": 5,
  "results": [
    {
      "id": 1,
      "issue_type": "HIGH_BOUNCE",
      "severity": "CRITICAL",
      "description": "...",
      "location_url": "...",
      "readable_location": "Главная страница",
      "impact_score": 8.5,
      "ai_hypothesis": "...",
      "ai_solution": "...",
      "trend": "worse",
      "priority": "HIGH"
    }
  ]
}
```

### Воронки

**GET** `/api/funnels/?version=1`
```json
{
  "count": 4,
  "results": [
    {
      "id": 1,
      "name": "Поиск рейтингов",
      "description": "...",
      "steps_count": 2,
      "is_preset": true,
      "has_metrics": true,
      "total_entered": 900,
      "total_completed": 450,
      "overall_conversion": 50.0
    }
  ]
}
```

**GET** `/api/funnels/<id>/by-cohorts/`
```json
{
  "funnel": {"id": 1, "name": "..."},
  "overall_metrics": {
    "total_entered": 900,
    "total_completed": 450,
    "overall_conversion": 50.0
  },
  "cohort_breakdown": {
    "Когорта 1": {
      "entered": 300,
      "completed": 180,
      "conversion": 60.0,
      "ai_analysis": "..."
    }
  }
}
```

### Когорты

**GET** `/api/cohorts/?version=1`
```json
{
  "cohorts": [
    {
      "name": "Активные исследователи",
      "percentage": 35.5,
      "avg_bounce_rate": 25.0,
      "avg_duration": 320,
      "users_count": 1065,
      "metrics": {...},
      "conversion_rates": {...}
    }
  ]
}
```

### Страницы

**GET** `/api/pages/?version=1&limit=50&min_views=20&order=-exit_rate`
```json
{
  "pages": [
    {
      "url": "https://priem.mai.ru/",
      "norm_url": "https://priem.mai.ru/",
      "readable": "Главная страница",
      "total_views": 1500,
      "unique_visitors": 800,
      "exit_rate": 45.2,
      "avg_time_on_page": 120,
      "dominant_device": "desktop",
      "dominant_cohort": "Активные исследователи"
    }
  ]
}
```

## 🎮 Команды управления

### Загрузка данных

```bash
docker-compose exec web python manage.py ingest_data \
    --visits "2022_yandex_metrika_visits.parquet" \
    --hits "2022_yandex_metrika_hits.parquet" \
    --product-version "v1.0 (2022)" \
    --year 2022
```

**Параметры**:
- `--visits` - путь к файлу visits
- `--hits` - путь к файлу hits
- `--product-version` - название версии
- `--year` - год данных
- `--clear` - очистить существующие данные версии перед загрузкой

### Создание preset-воронок

```bash
docker-compose exec web python manage.py create_funnels \
    --product-version "v1.0 (2022)"
```

Создает предустановленные воронки на основе популярных путей пользователей.

### Расчет метрик воронок

```bash
# Общие метрики
docker-compose exec web python manage.py calculate_funnels \
    --product-version "v1.0 (2022)"

# С разбивкой по когортам
docker-compose exec web python manage.py calculate_funnels \
    --product-version "v1.0 (2022)" \
    --by-cohorts

# Для конкретной воронки
docker-compose exec web python manage.py calculate_funnels \
    --product-version "v1.0 (2022)" \
    --funnel-id 1
```

### Автоматическое обнаружение воронок

```bash
docker-compose exec web python manage.py discover_funnels \
    --product-version "v1.0 (2022)" \
    --min-support 15 \
    --min-percentage 0.5 \
    --max-funnels 10
```

**Параметры**:
- `--min-support` - минимальное количество сессий для последовательности
- `--min-percentage` - минимальный процент от общего числа сессий
- `--max-funnels` - максимальное количество воронок для создания
- `--min-length` / `--max-length` - длина последовательности (по умолчанию 2-4)

### Запуск только анализа проблем

```bash
docker-compose exec web python manage.py run_analysis_only \
    --product-version "v1.0 (2022)" \
    --clear-existing
```

Полезно, если данные уже загружены, но нужно пересчитать только проблемы.

### Проверка статуса загрузки

```bash
docker-compose exec web python manage.py check_ingestion_status
```

Показывает:
- Количество загруженных сессий, hits, метрик
- Количество обнаруженных проблем
- На каком этапе остановилась загрузка (если была прервана)

### Обновление AI-инсайтов

```bash
docker-compose exec web python manage.py refresh_ai \
    --product-version "v1.0 (2022)" \
    --issue-type HIGH_BOUNCE
```

Перегенерирует AI-гипотезы и решения для существующих проблем.

## 📁 Структура проекта

```
ux-insight/
├── analytics/                    # Основное Django-приложение
│   ├── management/
│   │   └── commands/            # Management команды
│   │       ├── ingest_data.py   # ETL + детекция проблем
│   │       ├── ingest_analysis.py # Детекторы UX-проблем
│   │       ├── create_funnels.py # Создание preset-воронок
│   │       ├── calculate_funnels.py # Расчет метрик воронок
│   │       ├── discover_funnels.py # Автообнаружение воронок
│   │       ├── run_analysis_only.py # Только анализ проблем
│   │       └── check_ingestion_status.py # Проверка статуса
│   ├── migrations/              # Миграции БД
│   ├── models.py               # Django модели
│   ├── views.py                # Главный файл views (импорты)
│   ├── views_dashboard.py      # Dashboard views
│   ├── views_compare.py        # Compare views
│   ├── views_issues.py         # Issues views
│   ├── views_funnels.py        # Funnels views (CRUD)
│   ├── views_api_extra.py      # Дополнительные API
│   ├── views_helpers.py        # Вспомогательные функции
│   ├── ai_service.py           # Интеграция с YandexGPT
│   ├── funnel_utils.py         # Утилиты для воронок
│   ├── funnel_discovery.py     # Автообнаружение воронок
│   ├── forms.py                # Формы для воронок
│   ├── utils.py                # Общие утилиты
│   ├── urls.py                 # URL маршруты
│   └── goals.yaml              # Конфигурация целей Метрики
├── templates/                   # Django шаблоны
│   ├── base.html
│   └── analytics/
│       ├── dashboard.html
│       ├── issues.html
│       ├── compare.html
│       ├── funnels.html
│       ├── funnel_detail.html
│       ├── funnel_create.html
│       ├── funnel_edit.html
│       └── funnel_delete.html
├── config/                      # Настройки Django
│   ├── settings.py
│   └── urls.py
├── docker-compose.yml          # Docker Compose конфигурация
├── Dockerfile                   # Docker образ
├── entrypoint.sh               # Entrypoint скрипт
├── requirements.txt            # Python зависимости
├── .env.example                # Пример переменных окружения
└── README.md                   # Этот файл
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная | По умолчанию |
|-----------|----------|--------------|--------------|
| `SECRET_KEY` | Django secret key | Да | - |
| `DEBUG` | Режим отладки | Нет | `False` |
| `DB_HOST` | Хост БД | Нет | `db` |
| `DB_PORT` | Порт БД | Нет | `5432` |
| `DB_NAME` | Имя БД | Нет | `postgres` |
| `DB_USER` | Пользователь БД | Нет | `postgres` |
| `DB_PASSWORD` | Пароль БД | Нет | `postgres` |
| `FOLDER_ID` | Yandex Cloud Folder ID | Нет | - |
| `API_KEY` | Yandex Cloud API Key | Нет | - |
| `AUTO_INGEST` | Автозагрузка данных | Нет | `1` |
| `AUTO_FUNNELS` | Автосоздание воронок | Нет | `1` |

### Конфигурация целей (`goals.yaml`)

Файл содержит конфигурацию целей Яндекс.Метрики:

```yaml
- code: apply_it_button
  name: 'Кнопка "Поступить" IT-магистратура'
  ym_goal_id: 39566071
  match: {type: identifier, value: APPLY_IT}

- code: contacts_view
  name: 'просмотр контактов'
  ym_goal_id: 53625076
  match: {type: url_prefix, value: 'https://mai.ru/priem/contacts.php'}
```

**Типы match**:
- `identifier` - проверка через `goalsID` в сессии
- `url_prefix` - URL начинается с префикса
- `url_contains` - URL содержит строку
- `click` - клик по элементу (требует специальной обработки)

## 🔍 Оптимизации производительности

### Batch-обработка
- VisitSession: batch_size=10000
- PageHit: batch_size=50000

### SQL-оптимизации
- Расчет `time_on_page` через SQL window functions (в 10-100 раз быстрее Python)
- Агрегация метрик страниц одним запросом

### Кэширование
- Метрики воронок кэшируются в `FunnelMetrics`
- Разделение общих и когортных метрик для быстрого доступа

### Ленивые вычисления
- Воронки рассчитываются отдельной командой (не блокируют основной ETL)
- AI-инсайты генерируются асинхронно

## 🐛 Решение проблем

### Проблема: Данные не загружаются

1. Проверьте наличие Parquet-файлов в корне проекта
2. Проверьте логи: `docker-compose logs web`
3. Проверьте статус: `docker-compose exec web python manage.py check_ingestion_status`

### Проблема: AI-инсайты не генерируются

1. Проверьте `FOLDER_ID` и `API_KEY` в `.env`
2. Проверьте доступность Yandex Cloud API
3. При отсутствии ключей используются заглушки (это нормально)

### Проблема: Воронки показывают 0 конверсию

1. Убедитесь, что воронки созданы: `create_funnels`
2. Рассчитайте метрики: `calculate_funnels`
3. Проверьте, что шаги воронки соответствуют реальным URL/целям в данных

### Проблема: Ошибки миграций

```bash
docker-compose exec web python manage.py migrate --fake-initial
```

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

Проект разработан в рамках хакатона PSB "AI Challenge: Banking".

---

**Версия документа**: 1.0  
**Последнее обновление**: 2025
