# Тестирование AI-анализа сравнения версий

## 🧪 Быстрый тест

### 1. Проверка через веб-интерфейс

```powershell
# Откройте в браузере
http://localhost:8000/analytics/compare/?v1=1&v2=2
```

**Что проверить:**
- ✅ В верхней части страницы появляется блок "🤖 AI-анализ сравнения"
- ✅ Анализ содержит разделы: Резюме, Улучшения, Проблемы, Рекомендация
- ✅ Текст анализа соответствует изменениям между версиями

### 2. Проверка через API

```powershell
# Получить сравнение с AI-анализом
curl "http://localhost:8000/analytics/api/compare/?v1=1&v2=2" | ConvertFrom-Json | Select-Object -ExpandProperty comparison | Select-Object ai_analysis
```

Или через PowerShell:

```powershell
$response = Invoke-RestMethod -Uri "http://localhost:8000/analytics/api/compare/?v1=1&v2=2"
$response.comparison.ai_analysis
```

**Что проверить:**
- ✅ Поле `ai_analysis` присутствует в ответе
- ✅ Содержит структурированный текст анализа
- ✅ Формат: "Резюме:", "Улучшения:", "Проблемы:", "Рекомендация:"

### 3. Проверка без AI (fallback)

Если AI недоступен (нет API ключей), должен работать fallback:

```powershell
# Временно отключить AI (убрать API_KEY)
docker-compose exec web python manage.py shell -c "
import os
os.environ.pop('API_KEY', None)
from analytics.views_compare import compare_versions
from django.test import RequestFactory
rf = RequestFactory()
request = rf.get('/analytics/compare/?v1=1&v2=2')
# Проверить что fallback работает
"
```

**Что проверить:**
- ✅ Анализ все равно появляется (rule-based fallback)
- ✅ Содержит базовую информацию о изменениях

## 📊 Ожидаемый результат

### Пример AI-анализа:

```
Резюме: Версия v2.0 (2024) показывает улучшение - снижение отказов на 3.2% и увеличение времени на сайте на 12.5с.
Улучшения: снижение отказов на 3.2%, увеличение времени на сайте на 12.5с, 5 проблем улучшилось, 3 проблем решено
Проблемы: 2 новых проблем
Рекомендация: Продолжить работу по улучшению UX, фокусируясь на проблемах с высоким impact_score.
```

## 🔍 Отладка

### Если AI-анализ не появляется:

1. **Проверьте API ключи**:
```powershell
docker-compose exec web python manage.py shell -c "
import os
print('FOLDER_ID:', os.environ.get('FOLDER_ID'))
print('API_KEY:', 'SET' if os.environ.get('API_KEY') else 'NOT SET')
"
```

2. **Проверьте логи**:
```powershell
docker-compose logs web | Select-String "AI|YandexAI"
```

3. **Проверьте наличие версий**:
```powershell
docker-compose exec web python manage.py shell -c "
from analytics.models import ProductVersion
for v in ProductVersion.objects.all():
    print(f'{v.id}: {v.name}')
"
```

### Если анализ пустой или некорректный:

1. **Проверьте данные для сравнения**:
   - Должно быть минимум 2 версии
   - Версии должны иметь данные (сессии, issues, страницы)

2. **Проверьте формат ответа AI**:
   - Должен содержать строки "Резюме:", "Улучшения:", "Проблемы:", "Рекомендация:"

## ✅ Чеклист тестирования

- [ ] AI-анализ появляется на странице сравнения
- [ ] Анализ содержит все 4 раздела (Резюме, Улучшения, Проблемы, Рекомендация)
- [ ] Анализ соответствует реальным изменениям между версиями
- [ ] API возвращает поле `ai_analysis`
- [ ] Fallback работает при отсутствии AI
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах Docker

